package com.example.myapplication2

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.os.PersistableBundle
import android.view.View
import android.widget.*
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView

class MainPage : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.themainpage)}}
        /*
    * design single item
    * recycleview activity
    * adapter
    *view holder
    * */
      /*  val dataset = generateDataOfBooK()
        val customlayoutManager = LinearLayoutManager(this)
        val customadapter = PhoneAdabter(dataset)
        val rvPhone = findViewById<RecyclerView>(R.id.RecycleViewOfMAinPageID)
        rvPhone.adapter = customadapter
        rvPhone.layoutManager = customlayoutManager

    }*/
    

          //  }
// dataset list of data (string,parent,student,movies,anything)private fun generateDataOfBooK():List<DataOfBook>{
/*

return mutableListOf(   DataOfBook (title = "sherif" , price = 30),
    DataOfBook (title = "hasssan", price = 30),
    DataOfBook (title = "sherif2", price = 30)
    )
}
*/
